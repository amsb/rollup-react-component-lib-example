A minimal example of creating a [React](https://reactjs.org/) component library with [rollup.js](https://rollupjs.org).

# Build Library

```
cd mylib
npm install
cd ..
```

# Run Example

```
npm install
npm start
```